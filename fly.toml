# fly.toml app configuration file generated for watch-out on 2024-02-19T00:29:06+08:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'watch-out'
primary_region = 'sin'

[build]

[deploy]
  release_command = "sh ./release.sh"

[env]
  PORT = "3000"
  USERNAME = "postgres"
  PASSWORD = "CWHOcJE0Xr5OLCB"
  DATABASE = "watch-out"
  HOST = "watch-out-db.internal"
  DIALECT = "postgres"
  NODE_ENV = "production"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  size = 'shared-cpu-1x'
